import React from "react";

export default function Count({itemsCount}){
    return <div style={{marginBottom : '10px', display: 'flex', justifyContent: 'center'}}>pending items count - {itemsCount}</div>

}